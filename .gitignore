target/
build_dev/
build/
build_mac/
dist/
temp/
.cache/
scripts/deploy.local
scripts/deploy.local.ps1
/.remote/
.superpowers/
.claude/
# 本地任务指引, 不入库
docs/superpowers/

# Swift 构建产物
wind_macos/.build/
.swiftpm/
build_debug/

# 五笔词库为 gen_dict 生成物, 只存在于 build 输出, 不入库
# (源在 .cache/rime-wubi/, 由 scripts/dev.* 的 gen-data 下载并生成)
data/schemas/wubi86/*.dict.yaml
